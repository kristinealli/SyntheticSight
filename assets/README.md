# Visual Assets

These plots were extracted from the **executed final training notebook** and the apparent-lightness audit so the README and documentation use the same run evidence as the project presentation.

- `resnet50_loss_curves.png` — training vs validation loss across epochs.
- `validation_metrics_by_epoch.png` — validation metric progression.
- `resnet50_validation_confusion_matrix.png` — best-checkpoint validation matrix.
- `resnet50_final_test_confusion_matrix.png` — locked-test confusion matrix.
- `resnet50_roc_pr_curves.png` — ROC and precision-recall curves.
- `validation_probability_distribution.png` — validation score distribution.
- `test_probability_distribution.png` — test score distribution.
- apparent-lightness plots — representation-audit outputs.

Do not replace these with plots from older intermediate runs without updating the corresponding metrics and documentation.
